# Avatar assets

Final avatar artwork should live here as transparent PNG files.

Recommended format:

- 256x256 PNG
- transparent background
- centered character with safe padding
- readable at 44px, 64px, and 96px

The current app uses emoji placeholders for `devil`, `joker`, `masked`, and `innocent` until final artwork is added.
